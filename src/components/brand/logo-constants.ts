/** Rainbow ring around the logo tile — must match programmatic brand art. */
export const LOGO_TILE_CONIC =
  "conic-gradient(from 140deg at 50% 50%, #43c067 0deg, #2f93de 70deg, #7c61ff 155deg, #e255a8 235deg, #ff9540 300deg, #f2d049 338deg, #43c067 360deg)";

/** Canonical lockup tagline (all caps, letter-spacing applied in Logo). */
export const LOGO_TAGLINE = "ADVENTURE PLANNING";

/** Matches `--tt-royal-deep` in globals.css — use where Tailwind tokens are unavailable (e.g. OG). */
export const LOGO_ROYAL_DEEP_HEX = "#173b7d";
